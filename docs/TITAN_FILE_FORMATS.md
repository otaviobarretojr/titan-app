# Formatos TITAN

Arquivos suportados: `.titanprofile`, `.titanworkout`, `.titannutrition`, `.titancardio`, `.titansupplements` e `.titanproject`. Todo arquivo é autoidentificável por cabeçalho interno `schema: TITAN`, `schemaVersion: 1.0`, `type`, `title`, `author`, `createdAt` e `payload`; a extensão nunca é a única fonte de confiança.
