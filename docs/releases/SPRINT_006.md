# Sprint 006 — Módulo Cardio

## Consolidação de qualidade

- O cálculo e a formatação de pace possuem cobertura unitária, inclusive para
  o arredondamento que converte 60 segundos no minuto seguinte.
- A suíte unitária pode ser executada de forma determinística com `npm run test`.
- As verificações previstas para a entrega são `npm run test` e
  `npm run validate`.
