import {describe,expect,it} from 'vitest'
import {addMacros,calculateMacros,calculatePurchaseQuantity,calculateRecipeYield,convertPreparedToRaw,convertRawToPrepared,normalizeNutritionLabel,roundDisplay,subtractPantry,sumWeeklyRequirements} from './nutritionCalculations'
import {compareSubstitution} from './substitutionEngine'
import type {FoodLibraryRecord} from '../types/foundation'
const food=(id:string,proteinG:number):FoodLibraryRecord=>({id,normalizedName:id,displayName:id,categoryId:'c',sourceId:'taco',sourceType:'taco',sourceFoodId:id,sourceVersion:'2011',sourceReference:'official',sourceAccessedAt:'2026-01-01',preparationState:'cooked',referenceQuantity:100,referenceUnit:'g',caloriesKcal:100,proteinG,carbohydrateG:10,fatG:2,dataConfidence:'official_database',isCustom:false,isVerified:true,isActive:true,createdAt:'x',updatedAt:'x'})
describe('nutrition calculations',()=>{it('scales and accumulates decimals',()=>{expect(calculateMacros(food('a',20),50).proteinG).toBe(10);expect(addMacros([{caloriesKcal:.1,proteinG:.1,carbohydrateG:.1,fatG:.1},{caloriesKcal:.2,proteinG:.2,carbohydrateG:.2,fatG:.2}]).proteinG).toBe(.3)});it('converts raw/prepared reversibly',()=>expect(convertPreparedToRaw(convertRawToPrepared(100,.8),.8)).toBe(100));it('normalizes a label and preserves caller label',()=>expect(normalizeNutritionLabel({brand:'b',productName:'p',servingDescription:'1',servingQuantity:25,servingUnit:'g',caloriesKcal:50,proteinG:5,carbohydrateG:3,fatG:2,labelCheckedAt:'x'}).proteinG).toBe(20));it('rounds display and purchases explicitly',()=>{expect(roundDisplay(1.26)).toBe(1.3);expect(calculatePurchaseQuantity(101,20,50)).toBe(100)});it('aggregates weekly and discounts pantry',()=>{expect(sumWeeklyRequirements([{foodId:'a',quantity:.1},{foodId:'a',quantity:.2}])[0].quantity).toBe(.3);expect(subtractPantry(10,12)).toBe(0)});it('calculates recipe yield',()=>expect(calculateRecipeYield([100,100],10)).toBe(180));it('reports substitution sources and differences',()=>{const result=compareSubstitution(food('a',20),food('b',10),100,'proteinG');expect(result?.substituteQuantity).toBe(200);expect(result?.originalSource).toBe('official')})})
